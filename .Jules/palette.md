## 2025-05-14 - Accessible Icon Buttons
**Learning:** Icon-only buttons (like AppBar actions and FloatingActionButtons) are not accessible to screen readers and lack visual cues for sighted users unless tooltips are explicitly provided.
**Action:** Always include a `tooltip` property for `IconButton` and `FloatingActionButton` to ensure they are accessible and provide a better UX.
